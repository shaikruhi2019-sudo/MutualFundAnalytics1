Data Quality Summary

1. Successfully fetched historical NAV data for six mutual fund schemes using the MFAPI API.
2. The downloaded datasets were saved as CSV files in the data/raw directory.
3. Each dataset contains two primary fields:
   - date
   - nav
4. No missing values were observed in the downloaded NAV datasets during initial inspection.
5. The date values are stored in DD-MM-YYYY format and may require conversion to datetime format during preprocessing.
6. The NAV values are stored as strings and should be converted to numeric (float) for analysis.
7. Column names are consistent across all downloaded datasets, making them suitable for combined analysis.
8. Validation of AMFI scheme codes against fund_master and nav_history datasets could not be performed because those datasets were not provided.
9. Further data quality checks, including duplicate detection, referential integrity, and category validation, will be performed once the complete project datasets are available.
